# Basic Controls sample

Add `BasicControlsReader` to any GameObject, then assign a `VirtualJoystick` and a
`MobileActionButton` from your Canvas.

The sample logs joystick and button input so each component can be inspected
without requiring a particular scene or character controller.
