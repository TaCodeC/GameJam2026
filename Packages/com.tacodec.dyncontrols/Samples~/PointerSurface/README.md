# Pointer Surface sample

Place `PointerSurface` on a transparent, raycastable UI Image covering the area
where dragging is allowed. Add `PointerDragExample` to another GameObject, then
assign the surface, Canvas, and a UI RectTransform to drag.

The assigned UI element can then be dragged with either a mouse or one finger.
